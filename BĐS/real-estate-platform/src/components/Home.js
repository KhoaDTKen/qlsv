import React from 'react';
import { Button, Typography } from '@mui/material';

const Home = () => {
  return (
    <div>
      <Typography variant="h2">Welcome to Our Real Estate Platform</Typography>
      <Button variant="contained" color="primary">
        Explore Listings
      </Button>
    </div>
  );
};

export default Home;
